transport:
  MQTT:
    type: "MQTT"
    display: "transport.mqtt"
    disabled: false
    order: 1
    params:
      private:
        brokerUrl: "mqtts://mqtt.dev.procivis-one.com:8883"
exchange:
  OPENID4VC:
    params:
      public:
        issuance:
          redirectUri:
            allowedSchemes: ["http", "https"]
        presentation:
          redirectUri:
            allowedSchemes: ["http", "https"]
  MDOC_OPENID4VP:
    params:
      public:
        issuance:
          redirectUri:
            allowedSchemes: ["http", "https"]
        presentation:
          redirectUri:
            allowedSchemes: ["http", "https"]
