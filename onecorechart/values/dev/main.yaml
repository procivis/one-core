baseUrl: "https://core.dev.one-trust-solution.com"

sentry:
  env: "dev"

ingress:
  hosts:
  - host: core.dev.one-trust-solution.com
    paths:
      - path: /
        pathType: ImplementationSpecific
