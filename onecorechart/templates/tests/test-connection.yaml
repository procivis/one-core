apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "onecorechart.fullname" . }}-test-connection"
  labels:
    {{- include "onecorechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "onecorechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
