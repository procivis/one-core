apiVersion: v1
kind: Service
metadata:
  name: {{ include "onecorechart.fullname" . }}
  labels:
    {{- include "onecorechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "onecorechart.selectorLabels" . | nindent 4 }}
